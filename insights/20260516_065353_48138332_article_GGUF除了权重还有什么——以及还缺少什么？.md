# GGUF格式深入解析：权重之外还有什么？

**洞察链接**：https://nobodywho.ooo/posts/whats-in-a-gguf/

**来源**：NobodyWho

**发布日期**：2026年5月14日

---

## 概述

GGUF是llama.cpp使用的模型文件格式，其核心优势在于**单文件**设计——将模型运行所需的所有元数据整合到一个文件中，相比safetensors仓库（需要多个JSON文件）或Ollama模型（多层OCI容器）更加简洁易用。

## 已包含的关键元数据

### 1. 聊天模板（Chat Templates）

对话模型训练时使用特定格式的序列，聊天模板使用Jinja2模板语言定义。不同模型的模板差异很大：
- Gemma4使用`<|turn|>user...<turn|>`格式
- LFM2使用`<|im_start|>user...<|im_end|>`格式

复杂模型还包含推理块格式化、工具调用、多媒体消息编码等功能。默认聊天模板存储在`tokenizer.chat_template`键中。

### 2. 特殊_tokens

特殊 tokens 具有超出其字面含义的语义，通常不对用户显示，但有文本表示。例如：
- `<eos>`：序列结束，触发停止生成
- `<bos>`：序列开始
- `<|tool_call>`/`<tool_call|>`：工具调用标记
- `<|turn>`/`<turn|>`：对话轮次标记

### 3. 采样器配置

语言模型输出下一个token的概率分布，采样器从中选择具体token。GGUF现在支持直接指定采样链顺序（`general.sampling.sequence`字段），这是其他格式（如Ollama的JSON和HF的`generation_config.json`）所缺少的。

## 仍然缺失的功能

### 1. 工具调用格式

目前每种模型都有硬编码的解析路径：
- Qwen3：`{"name": "...", "arguments": {...}}`
- Qwen3.5：XML风格格式
- Gemma4：`call:name{field:<|"|>value<|"|>}`

建议在GGUF中包含可导出解析器的语法定义。NobodyWho更进一步，为每个工具生成约束语法，确保类型安全。

### 2. 思考_tokens（Think Tokens）

上游HF仓库已开始包含`think_token`字段用于标记思考过程，但下游GGUF转换通常不包含此字段，导致无法分离思考流和主输出。

### 3. 投影模型（Projection Models）

多模态LLM需要额外的投影模型处理图像和音频，目前需要传入两个GGUF文件。建议将投影模型权重和配置打包到主GGUF文件中，或提供带/不带投影权重的两种变体。

### 4. 支持功能列表

无法从GGUF文件直接检测模型支持的功能（如图像输入、工具调用、思考块）。建议添加功能标志，使推理库能更一致地提供错误提示和警告。

## 结论

GGUF的优势在于其单一文件设计，涵盖了正确运行模型所需的所有元数据，且作为开放可扩展格式拥有强大的社区支持。作者希望GGUF社区继续加强标准，保持出色的开发者体验，同时便于在应用中对模型进行替换。